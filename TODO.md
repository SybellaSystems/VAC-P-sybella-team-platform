as a Project Creation Wizard, where every step builds the project gradually, validates information, and automatically creates records in other modules.

VAC-P
Project Management Module

Every significant change should be versioned with who changed it, when, the previous value, the new value, and an optional reason. Financial changes should immediately synchronize with the Finance module, document uploads should be classified into the project repository, and team changes should update HR capacity planning.